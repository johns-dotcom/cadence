# g-auth-screens — see pages/login.md

The auth-screens global surface (login, invite acceptance, reset flow, suspended-workspace
messaging) was audited together with the Login page. Full 7-section comparison and defect
table: `pages/login.md`. Its defects are logged under `## login` in `_defects-raw.md`;
no separate `## g-auth-screens` block exists (deliberate — one surface, one block).
